{
  "name": "lei-ns",
  "main": "./lib/index.js",
  "files": [
    "lib"
  ],
  "version": "0.4.0",
  "description": "Organizing your code without writing wired variable constructs and helper objects",
  "keywords": [
    "hot",
    "reload"
  ],
  "author": "Zongmin Lei <leizongmin@gmail.com> (http://ucdok.com)",
  "contributors": [
    {
      "name": "Zongmin Lei",
      "email": "leizongmin@gmail.com"
    }
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/leizongmin/node-lei-ns.git"
  },
  "licenses": "MIT",
  "bugs": {
    "url": "https://github.com/leizongmin/node-lei-ns/issues"
  },
  "engines": {
    "node": ">= 0.10"
  },
  "dependencies": {},
  "devDependencies": {
    "conventional-changelog": "^1.1.0",
    "coveralls": "^2.11.6",
    "eslint": "^3.3.1",
    "eslint-config-lei": "0.0.14",
    "istanbul": "^0.4.2",
    "mocha": "^2.3.4"
  },
  "eslintConfig": {
    "extends": "lei/mocha"
  },
  "scripts": {
    "test-cov": "istanbul cover _mocha -- -t 5000 -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage",
    "test": "mocha -t 5000",
    "changelog": "./node_modules/.bin/conventional-changelog -p angular -i CHANGELOG.md -w -r 0",
    "lint": "eslint lib test --fix && echo 'eslint passed'",
    "prepublish": "npm run lint && npm run test"
  }
}
